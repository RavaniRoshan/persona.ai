{
  "name": "@personamirror/persona-schema",
  "version": "1.0.0",
  "description": "TypeScript types and validation for persona management",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "test": "jest",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "typescript": "^5.2.2",
    "zod": "^3.22.4"
  }
}
